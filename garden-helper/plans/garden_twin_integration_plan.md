# Garden Twin Integration Plan

Implemented locally in `garden-helper` as a prototype:

- canonical snapshot schema as system of record
- static planner fork with snapshot import/export
- xardin-compatible SQLite memory plus geometry sidecars
- sync adapter for planner snapshot import/export
- VeggieTools enrichment plus cached OpenFarm metadata lookups

This is the decision-complete plan artifact requested for `garden-helper/plans`.
